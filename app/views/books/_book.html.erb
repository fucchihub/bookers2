   <h2>New book</h2>

      <% if book.errors.any? %>
        <p><%= pluralize(book.errors.count, "error") %> prohibited your book from being saved:</p>
        <ul>
          <% book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>

      <%= form_with model: book, url: books_path, method: :post do |f| %>
        <label for="inputNewTitle" class="form-label">Title</label>
        <p><%= f.text_field :title, id:"inputNewTitle", class: "form-control" %></p>

        <label for="inputNewOpinion" class="form-label">Opinion</label>
        <p><%= f.text_area :body, id:"inputNOpinion", class: "form-control" %></p>


        <p><%= f.submit 'Create Book', class: 'btn bg-success text-white' %></p>
      <% end %>


<%#<%= render 'users/list', user: @user %>
<%#<%= render 'books/book', book: @book %>
